# Anthropic API key — get yours at https://console.anthropic.com
# If set here, the sidebar key input is hidden (useful for hosted deployments).
# If not set, the app will prompt the user to enter their key in the UI.
ANTHROPIC_API_KEY=sk-ant-...
